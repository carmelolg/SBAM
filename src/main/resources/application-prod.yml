# Custom properties
environment: prod
name: Production

# Spring configurations
spring:
  devtools:
    restart:
      enabled: false

# Logging properties
logging:
  level:
    it.carmelolagamba: INFO
    org.springframework: WARN